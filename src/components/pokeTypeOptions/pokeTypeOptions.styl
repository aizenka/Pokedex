.poke-options {
  select {
    background-color: transparent;
    outline: none;
    border: none;
    color: #fff;
    &:hover {
      cursor: pointer;
    }
    option {
      background: #c10000;
    }
  }
  span {
    color: #fff;
  }
}
